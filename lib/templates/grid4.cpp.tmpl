int dx[] = {0, 1, 0, -1};
int dy[] = {1, 0, -1, 0};
EDULCNI_VIS(edulcni::live::array("grid4.dx", dx));
EDULCNI_VIS(edulcni::live::array("grid4.dy", dy));
EDULCNI_STEP("Four-neighbor directions initialized");
auto inside = [&](int x, int y) {
	return x >= 0 && x < n && y >= 0 && y < m;
};
