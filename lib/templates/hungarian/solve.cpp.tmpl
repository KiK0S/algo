void {{solveName}}() {
  int {{rowCountName}}, {{colCountName}};
  cin >> {{rowCountName}} >> {{colCountName}};
  std::vector<std::vector<{{costType}}>> {{sourceName}}({{rowCountName}}, std::vector<{{costType}}>({{colCountName}}));
  for (int i = 0; i < {{rowCountName}}; ++i) {
    for (int j = 0; j < {{colCountName}}; ++j) {
      cin >> {{sourceName}}[i][j];
    }
  }
  auto {{resultName}} = {{callName}}({{sourceName}});
  cout << {{resultName}}.min_cost << '\n';
  for (int i = 0; i < static_cast<int>({{resultName}}.match_left.size()); ++i) {
    if (i > 0) {
      cout << ' ';
    }
    cout << {{resultName}}.match_left[i];
  }
  cout << '\n';
}
