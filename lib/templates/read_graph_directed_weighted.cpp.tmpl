vector<vector<pair<int, ll>>> g(n);
forn(i, m) {
	int a, b;
	ll w;
	cin >> a >> b >> w;
	--a, --b;
	g[a].push_back({b, w});
}
EDULCNI_VIS(edulcni::live::weighted_graph("input.graph", g));
EDULCNI_STEP("Directed weighted graph read");
